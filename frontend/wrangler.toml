# Cloudflare Pages 配置（公开模板）
# 复制 wrangler.local.toml 并修改为你的真实值

# 项目名称
name = "domain-monitor"

# Pages 构建输出目录
pages_build_output_dir = "./dist"

# Pages 环境变量（公开，非敏感）
[vars]
VITE_API_BASE_URL = "https://your-worker.your-domain.workers.dev"
